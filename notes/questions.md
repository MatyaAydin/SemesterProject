# Questions


* On which datasets should I focus on (show the ones that I found) ? The ones used in the papers or the ones implemented in Pytorch geometric temporal ?
* With only the energy consumptions (i.e `LD2011_2014.txt`), how to construct a graph ? If I do edges based on consumption similarity, is it not exactly redundant temporal correlation ?
* Which GNN architecture should I benchmark ?